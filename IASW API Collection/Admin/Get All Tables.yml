info:
  name: Get All Tables
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/admin/tables"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: "Get data from all main tables for debugging. WARNING: For development only."
