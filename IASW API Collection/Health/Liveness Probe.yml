info:
  name: Liveness Probe
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/health/live"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Kubernetes-style liveness probe. Returns 200 if application is alive.
