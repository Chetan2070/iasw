info:
  name: Get Customer by Account
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/customers/account/1234567890"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a customer by their account number.
