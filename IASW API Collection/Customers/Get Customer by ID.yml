info:
  name: Get Customer by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/customers/CUST001"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Get a specific customer by their customer ID.
