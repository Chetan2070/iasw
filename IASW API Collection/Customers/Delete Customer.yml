info:
  name: Delete Customer
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/customers/CUST-NEW001"
  headers:
    - name: Authorization
      value: Bearer {{access_token}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: Delete a customer record.
